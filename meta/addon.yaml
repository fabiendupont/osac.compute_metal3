name: osac-compute-metal3
display_name: Compute (Metal3)
description: Bare-metal provisioning via Metal3/Ironic for OSAC
version: 0.1.0

dependencies:
  osac_core: ">=0.1.0"

resource_types:
  - name: ComputeInstance
    scope: tenant
